import { FormatEquipmentPipe } from './format-equipment.pipe';

describe('FormatEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
