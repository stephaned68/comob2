import { FormatNotesPipe } from './format-notes.pipe';

describe('FormatNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
