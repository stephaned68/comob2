import { FormatEquipNotesPipe } from './format-equip-notes.pipe';

describe('FormatEquipNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEquipNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
